# Testing

The library's test suite verifies the functionality of the local storage operations, including reading, writing, and removing data. It uses the Amen testing framework to run asynchronous assertions against the filesystem.

You run the test suite by executing the `genie test` command.

```shell
npx genie test
```
